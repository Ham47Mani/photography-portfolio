import Image from "next/image"

const Footer = () => {
  return (
    <footer className="py-4 flex items-center justify-center">
      <p className="text-xl font-semibold">Photography Portfolios</p>
    </footer>
  )
}

export default Footer